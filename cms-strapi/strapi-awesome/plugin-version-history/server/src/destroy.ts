// import type { Strapi } from '@strapi/strapi';

// export const destroy = ({ strapi }: { strapi: Strapi }) => {
export const destroy = ({ strapi }) => {
  // destroy phase
};

export default destroy;
